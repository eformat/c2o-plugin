apiVersion: v1
kind: Service
metadata:
  name: {{ include "c2o-plugin.fullname" . }}
  labels:
    {{- include "c2o-plugin.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.tls.enabled }}
    service.alpha.openshift.io/serving-cert-secret-name: {{ include "c2o-plugin.fullname" . }}-tls
    {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.plugin.port }}
      protocol: TCP
  selector:
    {{- include "c2o-plugin.selectorLabels" . | nindent 4 }}
